/* Main Include */
#include "main.h"

int main(void)
{
	/* Configure HAL Driver, MCU Clock, and MCU Peripherals */
	AppSystemConfig();

	/* Init App with default configuration */
	AppSystemDefault();

    /* Loop forever */
	while(1)
	{

	}
}
